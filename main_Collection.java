package java_collections;

import java.util.HashSet;
import java.util.Iterator;
import java.util.LinkedHashSet;
import java.util.Set;
import java.util.TreeSet;

//collections-set of classes + interfaces used to store and manipulate groups of data.
//Iterable->collection->list,set,queue
public class main_Collection {
     public static void main(String [] args) {
    	 //set->hashset,linked hashset,treeset
    	 
    /*	 Set<Integer> nums=new TreeSet();//sorted+unique
    	 nums.add(7);
    	 nums.add(2);
    	 nums.add(5);
    	 

    	 
    	 Set<Integer> ls=new LinkedHashSet();//maintaine sequence +unique
    	 nums.add(7);
    	 nums.add(2);
    	 nums.add(5);
    	 
    	 Set<Integer> hs=new HashSet();//unordered+unique
    	 nums.add(7);
    	 nums.add(2);
    	 nums.add(5);
    	 
    	 Iterator<Integer>it=nums.iterator();
    	 while(it.hasNext()) {
    		 System.out.println(it.next());
    	 }
        	*/
    	 
    //maps	 
    	 Maps maps=new Maps();//reffer to Maps class in this package
    	 maps.mapit();
    	 
    	 
    	 
      }
}
